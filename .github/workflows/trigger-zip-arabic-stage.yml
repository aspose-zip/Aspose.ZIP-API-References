name: Trigger ZIP Arabic Stage Build

on:
  push:
    branches: [ main ]
    paths: [ 'arabic/**' ]
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_TOKEN }}
          repository: Aspose/apireference.aspose.com
          event-type: build-zip-arabic-stage
